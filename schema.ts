import {sqliteTable,text,integer} from 'drizzle-orm/sqlite-core';
export const products=sqliteTable('products',{id:text('id').primaryKey(),code:text('code').notNull(),name:text('name').notNull(),description:text('description').notNull().default(''),category:text('category').notNull().default(''),price:integer('price'),unit:text('unit').notNull().default(''),image:text('image').notNull().default(''),qboId:text('qbo_id'),active:integer('active').notNull().default(1),approved:integer('approved').notNull().default(0),matchStatus:text('match_status').notNull().default('awaiting_quickbooks'),updatedAt:text('updated_at').notNull()});
export const customers=sqliteTable('customers',{id:text('id').primaryKey(),name:text('name').notNull(),email:text('email').notNull().default(''),phone:text('phone').notNull().default(''),address:text('address').notNull().default('{}'),active:integer('active').notNull().default(1)});
export const staff=sqliteTable('staff',{email:text('email').primaryKey(),role:text('role').notNull(),active:integer('active').notNull().default(1)});
export const orders=sqliteTable('orders',{id:text('id').primaryKey(),requestId:text('request_id').notNull().unique(),salesperson:text('salesperson').notNull(),customerId:text('customer_id').notNull(),customerName:text('customer_name').notNull(),lines:text('lines').notNull(),address:text('address').notNull(),notes:text('notes').notNull().default(''),subtotal:integer('subtotal').notNull(),status:text('status').notNull().default('submitted'),createdAt:text('created_at').notNull()});
export const settings=sqliteTable('settings',{key:text('key').primaryKey(),value:text('value').notNull()});
export const oauthStates=sqliteTable('oauth_states',{id:text('id').primaryKey(),userId:text('user_id').notNull(),expires:integer('expires').notNull()});
