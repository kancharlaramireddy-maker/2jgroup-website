const I18N = {
  en: {
    pageTitle: '2J Group Wholesale Desk', brandSubtitle: 'Wholesale beauty desk', sampleCatalog: 'Drive catalog linked', manageProducts: 'Manage products', order: 'Order',
    fieldSalesCatalog: 'FIELD SALES CATALOG', heroTitle: 'Find it. Show it. Add it.', heroBody: 'Build a shop order in minutes, then create a clean handoff for the back office.',
    currentOrder: 'Current order', currentOrderUpper: 'CURRENT ORDER', noProductsSelected: 'No products selected', askCatalog: 'Ask the catalog', searchPlaceholder: 'Try “oils under $15” or “best sellers for braids”', search: 'Search',
    sortProducts: 'Sort products', featured: 'Featured', priceLowHigh: 'Price: low to high', priceHighLow: 'Price: high to low', name: 'Name', wholesaleCatalog: 'Wholesale catalog', noMatches: 'No matching products', broaderSearch: 'Try a broader description or clear the filters.', clearSearch: 'Clear search',
    orderSummary: 'Order summary', retailShop: 'Retail shop', shopName: 'Shop name', contact: 'Contact', namePhone: 'Name / phone', salesperson: 'Salesperson', yourName: 'Your name', orderEmpty: 'Your order is empty', addFromCatalog: 'Add products from the catalog.', units: 'Units', estimatedTotal: 'Estimated total', orderNote: 'Order note', notePlaceholder: 'Delivery notes or special requests', reviewHandoff: 'Review handoff', quickbooksSafe: 'Prototype only — no order or invoice is sent to QuickBooks.',
    backOfficeHandoff: 'BACK-OFFICE HANDOFF', orderReady: 'Order ready to share', copySummary: 'Copy summary', downloadCsv: 'Download CSV', downloadTxt: 'Download .txt', catalogAdmin: 'CATALOG ADMIN', manageSampleProducts: 'Manage products', enrollProduct: 'Enroll a product', editProduct: 'Edit product', productName: 'Product name', productExample: 'e.g. Curl Defining Cream', category: 'Category', wholesalePrice: 'Wholesale price', description: 'Description', descriptionPlaceholder: 'Product details visible in the catalog photo', productPhoto: 'Product picture', tags: 'Tags', tagsExample: 'curl, moisture, salon', addCatalog: 'Add to catalog', saveChanges: 'Save changes', cancelEdit: 'Cancel edit', savedBrowser: 'Saved in this browser for prototype testing.', activeProducts: 'Active products', adminSearchPlaceholder: 'Find by item number or description',
    catHairCare: 'Hair Care', catWigEssentials: 'Wig Essentials', catStyling: 'Styling', catJewelry: 'Jewelry', catAccessories: 'Accessories', all: 'All', productCount: '{count} product(s)', sampleImage: 'SAMPLE IMAGE', driveImage: 'DRIVE CATALOG', onlyLeft: 'Only {count} left', inStock: '{count} in stock', catalogItem: 'Catalog item', perUnit: '/ unit', perPack: '/ pack', perDozen: '/ dozen', perDisplayPack: '/ display pack', pricePending: 'Price not readable', added: 'Added · {count}', add: '+ Add', selectedUnits: '{count} unit(s) selected', each: '{price} each', packPrice: '{price} {unit}', removeOne: 'Remove one {name}', addOne: 'Add one {name}', productAdded: '{name} added',
    searchFound: '✦ I found {count} catalog item(s) matching “{query}”{priceText}.', priceLimit: ' within the {price} limit', searchNone: '✦ I couldn’t find an exact catalog match for “{query}”. Try a category, use, or price limit.', notProvided: 'Not provided', created: 'Created', handoffTitle: '2J GROUP — WHOLESALE ORDER HANDOFF', estimatedTotalUpper: 'ESTIMATED TOTAL', note: 'NOTE', none: 'None', handoffDisclaimer: 'Prototype handoff only. Back office should verify pricing and create the invoice in QuickBooks.', product: 'Product', qty: 'Qty', unit: 'Unit', lineTotal: 'Line total', nextStep: 'Next step', nextStepText: 'Back office verifies the order and manually creates the QuickBooks invoice. No external system has been changed.', productCountAdmin: '{count} products', edit: 'Edit', delete: 'Delete', productEnrolled: 'Product enrolled in catalog', productUpdated: 'Product updated', productDeleted: '{name} deleted', summaryCopied: 'Order summary copied', summaryDownloaded: 'Order summary downloaded', csvDownloaded: 'Invoice-ready CSV downloaded'
  },
  ko: {
    pageTitle: '2J 그룹 도매 주문 데스크', brandSubtitle: '뷰티 제품 도매 주문', sampleCatalog: 'Drive 카탈로그 연결됨', manageProducts: '제품 관리', order: '주문',
    fieldSalesCatalog: '현장 영업 카탈로그', heroTitle: '찾고, 보여주고, 주문에 담으세요.', heroBody: '몇 분 안에 매장 주문을 만들고, 사무실 팀에 전달할 깔끔한 주문서를 준비하세요.',
    currentOrder: '현재 주문', currentOrderUpper: '현재 주문', noProductsSelected: '선택한 제품이 없습니다', askCatalog: '카탈로그에 질문하기', searchPlaceholder: '“15달러 이하 오일” 또는 “브레이드 베스트셀러”를 검색해 보세요', search: '검색',
    sortProducts: '제품 정렬', featured: '추천순', priceLowHigh: '가격: 낮은 순', priceHighLow: '가격: 높은 순', name: '이름순', wholesaleCatalog: '도매 카탈로그', noMatches: '일치하는 제품이 없습니다', broaderSearch: '검색어를 넓히거나 필터를 초기화해 보세요.', clearSearch: '검색 초기화',
    orderSummary: '주문 요약', retailShop: '소매점', shopName: '매장 이름', contact: '연락처', namePhone: '이름 / 전화번호', salesperson: '영업 담당자', yourName: '담당자 이름', orderEmpty: '주문이 비어 있습니다', addFromCatalog: '카탈로그에서 제품을 추가하세요.', units: '수량', estimatedTotal: '예상 합계', orderNote: '주문 메모', notePlaceholder: '배송 메모 또는 특별 요청', reviewHandoff: '전달 주문서 확인', quickbooksSafe: '프로토타입 전용 — QuickBooks로 주문이나 청구서가 전송되지 않습니다.',
    backOfficeHandoff: '사무실 전달 주문서', orderReady: '공유할 주문서가 준비되었습니다', copySummary: '주문서 복사', downloadCsv: 'CSV 다운로드', downloadTxt: '.txt 다운로드', catalogAdmin: '카탈로그 관리', manageSampleProducts: '제품 관리', enrollProduct: '제품 등록', editProduct: '제품 수정', productName: '제품명', productExample: '예: 컬 디파이닝 크림', category: '카테고리', wholesalePrice: '도매 가격', description: '설명', descriptionPlaceholder: '카탈로그 사진에 표시된 제품 정보', productPhoto: '제품 사진', tags: '태그', tagsExample: '컬, 보습, 살롱', addCatalog: '카탈로그에 추가', saveChanges: '변경사항 저장', cancelEdit: '수정 취소', savedBrowser: '프로토타입 테스트를 위해 이 브라우저에 저장됩니다.', activeProducts: '활성 제품', adminSearchPlaceholder: '품목 번호 또는 설명으로 찾기',
    catHairCare: '헤어 케어', catWigEssentials: '가발 필수품', catStyling: '스타일링', catJewelry: '주얼리', catAccessories: '액세서리', all: '전체', productCount: '제품 {count}개', sampleImage: '샘플 이미지', driveImage: 'DRIVE 카탈로그', onlyLeft: '{count}개 남음', inStock: '재고 {count}개', catalogItem: '카탈로그 제품', perUnit: '/ 개', perPack: '/ 팩', perDozen: '/ 다스', perDisplayPack: '/ 디스플레이 팩', pricePending: '가격 확인 필요', added: '담김 · {count}', add: '+ 담기', selectedUnits: '총 {count}개 선택', each: '개당 {price}', packPrice: '{price} {unit}', removeOne: '{name} 1개 빼기', addOne: '{name} 1개 추가', productAdded: '{name}을(를) 담았습니다',
    searchFound: '✦ “{query}”과(와) 일치하는 카탈로그 제품 {count}개를 찾았습니다{priceText}.', priceLimit: ' ({price} 이하)', searchNone: '✦ “{query}”과(와) 정확히 일치하는 카탈로그 제품이 없습니다. 카테고리, 용도 또는 가격 조건을 바꿔 보세요.', notProvided: '입력되지 않음', created: '작성일', handoffTitle: '2J 그룹 — 도매 주문 전달서', estimatedTotalUpper: '예상 합계', note: '메모', none: '없음', handoffDisclaimer: '프로토타입 전달서입니다. 사무실 팀에서 가격을 확인한 후 QuickBooks 청구서를 생성해야 합니다.', product: '제품', qty: '수량', unit: '단가', lineTotal: '합계', nextStep: '다음 단계', nextStepText: '사무실 팀에서 주문을 확인하고 QuickBooks 청구서를 직접 생성합니다. 외부 시스템은 변경되지 않았습니다.', productCountAdmin: '제품 {count}개', edit: '수정', delete: '삭제', productEnrolled: '카탈로그에 제품을 등록했습니다', productUpdated: '제품을 수정했습니다', productDeleted: '{name}을(를) 삭제했습니다', summaryCopied: '주문서를 복사했습니다', summaryDownloaded: '주문서를 다운로드했습니다', csvDownloaded: '청구서용 CSV를 다운로드했습니다'
  }
};

const BASE_PRODUCTS = [
  { id: 'p1', name: 'Nourishing Hair Oil', nameKo: '영양 헤어 오일', category: 'Hair Care', price: 11.5, stock: 48, icon: '🧴', color: '#d9b56e', tags: ['dry hair','moisture','best seller'], tagsKo: ['건조 모발','보습','베스트셀러'] },
  { id: 'p2', name: 'Braiding Shine Spray', nameKo: '브레이딩 샤인 스프레이', category: 'Styling', price: 8.75, stock: 21, icon: '✨', color: '#a7c6bc', tags: ['braids','shine','salon'], tagsKo: ['브레이드','윤기','살롱'] },
  { id: 'p3', name: 'Deluxe Wig Cap · 12 Pack', nameKo: '디럭스 위그 캡 · 12개입', category: 'Wig Essentials', price: 14, stock: 64, icon: '🧢', color: '#cfaa96', tags: ['wig cap','bulk','neutral'], tagsKo: ['위그 캡','대용량','뉴트럴'] },
  { id: 'p4', name: 'Edge Control Gel', nameKo: '엣지 컨트롤 젤', category: 'Styling', price: 7.25, stock: 9, icon: '🫙', color: '#b9a9cf', tags: ['edges','hold','best seller'], tagsKo: ['엣지','고정','베스트셀러'] },
  { id: 'p5', name: 'Gold Hoop Set', nameKo: '골드 후프 세트', category: 'Jewelry', price: 9.5, stock: 17, icon: '✨', color: '#d9c070', tags: ['earrings','gold','display'], tagsKo: ['귀걸이','골드','진열'] },
  { id: 'p6', name: 'Satin Bonnet · Assorted', nameKo: '새틴 보닛 · 혼합 색상', category: 'Wig Essentials', price: 6.5, stock: 35, icon: '👒', color: '#c995a9', tags: ['night care','assorted','satin'], tagsKo: ['나이트 케어','혼합','새틴'] },
  { id: 'p7', name: 'Rosemary Scalp Oil', nameKo: '로즈마리 두피 오일', category: 'Hair Care', price: 13.75, stock: 12, icon: '🌿', color: '#9caf82', tags: ['scalp','growth','natural'], tagsKo: ['두피','성장','내추럴'] },
  { id: 'p8', name: 'Setting Mousse', nameKo: '세팅 무스', category: 'Styling', price: 10.25, stock: 26, icon: '🫧', color: '#8fb8ca', tags: ['curls','hold','foam'], tagsKo: ['컬','고정','폼'] },
  { id: 'p9', name: 'Crystal Pin Collection', nameKo: '크리스털 핀 컬렉션', category: 'Jewelry', price: 12, stock: 7, icon: '💎', color: '#c3b6d6', tags: ['hair pins','crystal','occasion'], tagsKo: ['헤어 핀','크리스털','행사'] },
  { id: 'drive-brh', driveCatalog: true, sourceCode: 'BRH', name: 'Waterproof Bucket Hats · Assorted', nameKo: '방수 버킷햇 · 혼합 색상', category: 'Accessories', price: 48, unitLabel: 'dozen', stock: null, image: 'assets/drive/waterproof-bucket-hats.jpg', color: '#d9d7cf', tags: ['bucket hat','waterproof','assorted','BRH'], tagsKo: ['버킷햇','방수','혼합 색상','BRH'] },
  { id: 'drive-sf24b', driveCatalog: true, sourceCode: 'SF24B', name: 'Premium Silk Long Scarves · Assorted', nameKo: '프리미엄 실크 롱 스카프 · 혼합', category: 'Accessories', price: 30, unitLabel: 'dozen', stock: null, image: 'assets/drive/premium-silk-scarves.jpg', color: '#ead9dd', tags: ['silk scarf','long scarf','assorted','SF24B'], tagsKo: ['실크 스카프','롱 스카프','혼합','SF24B'] },
  { id: 'drive-ak60g', driveCatalog: true, sourceCode: 'AK60G', name: 'Starlight Anklet Display Pack · Assorted', nameKo: '스타라이트 발찌 디스플레이 팩 · 혼합', category: 'Jewelry', price: 60, unitLabel: 'displayPack', stock: null, image: 'assets/drive/starlight-anklets.jpg', color: '#c6c1dc', tags: ['anklet','starlight','display pack','AK60G'], tagsKo: ['발찌','스타라이트','디스플레이 팩','AK60G'] },
  { id: 'drive-tb15001', driveCatalog: true, sourceCode: 'TB15001', name: 'Premium Classic Turbans · Assorted', nameKo: '프리미엄 클래식 터번 · 혼합 색상', category: 'Accessories', price: 18, unitLabel: 'dozen', stock: null, image: 'assets/drive/premium-turbans.jpg', color: '#e6d2d1', tags: ['turban','head wrap','assorted','TB15001'], tagsKo: ['터번','헤드 랩','혼합 색상','TB15001'] },
  ...(typeof DRIVE_PRODUCTS === 'undefined' ? [] : DRIVE_PRODUCTS)
];

const LEGACY_SAMPLE_IDS = new Set(BASE_PRODUCTS.map(product => product.id));
const DEFAULT_DOZEN_CATEGORIES = new Set(['J', 'H', 'HR', 'HB', 'AK', 'E', 'EM', 'EC']);
const catalogProducts = (typeof DRIVE_PRODUCTS === 'undefined' ? [] : DRIVE_PRODUCTS).map(product => (
  product.pricePending && DEFAULT_DOZEN_CATEGORIES.has(product.category)
    ? { ...product, price: 7.5, pricePending: false, unitLabel: 'dozen', categoryDefaultPrice: true }
    : product
));
const savedProducts = JSON.parse(localStorage.getItem('2j-products') || 'null');
const savedOverrides = JSON.parse(localStorage.getItem('2j-product-overrides') || '{}');
const deletedProductIds = JSON.parse(localStorage.getItem('2j-deleted-products') || '[]');
const savedCustomProducts = (savedProducts || []).filter(product => String(product.id).startsWith('custom-') && !LEGACY_SAMPLE_IDS.has(product.id));
const seedProducts = [
  ...savedCustomProducts,
  ...catalogProducts.filter(product => !deletedProductIds.includes(product.id) && !savedCustomProducts.some(saved => saved.id === product.id)).map(product => ({ ...product, ...(savedOverrides[product.id] || {}) }))
];
const products = seedProducts.map(product => {
  const base = catalogProducts.find(item => item.id === product.id) || {};
  return { ...base, ...product, nameKo: product.nameKo || base.nameKo || product.name, tagsKo: product.tagsKo || base.tagsKo || product.tags || [] };
});
const state = { products, overrides: savedOverrides, cart: JSON.parse(localStorage.getItem('2j-cart') || '{}'), category: 'All', search: '', adminSearch: '', sort: 'featured', lastSummary: '', editingProductId: null, lang: localStorage.getItem('2j-lang') === 'ko' ? 'ko' : 'en' };
const $ = selector => document.querySelector(selector);
const t = (key, vars = {}) => String(I18N[state.lang][key] ?? I18N.en[key] ?? key).replace(/\{(\w+)\}/g, (_, name) => vars[name] ?? '');
const money = number => new Intl.NumberFormat(state.lang === 'ko' ? 'ko-KR' : 'en-US', { style: 'currency', currency: 'USD' }).format(number);
const escapeHtml = (value='') => String(value).replace(/[&<>'"]/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[char]));
const productName = product => state.lang === 'ko' ? (product.nameKo || product.name) : product.name;
const productTags = product => state.lang === 'ko' ? (product.tagsKo || product.tags) : product.tags;
const productDescription = product => {
  if(state.lang==='ko')return product.descriptionKo||product.description||`${product.sourceCode||product.name} · ${product.category} 카탈로그 제품입니다. 사진에서 스타일과 포장을 확인하세요.`;
  return product.description||`${product.sourceCode||product.name} · ${product.category} catalog item. See photo for style and packaging.`;
};
const CATEGORY_KEYS = {'All':'all','Hair Care':'catHairCare','Wig Essentials':'catWigEssentials','Styling':'catStyling','Jewelry':'catJewelry','Accessories':'catAccessories'};
const categoryName = category => CATEGORY_KEYS[category] ? t(CATEGORY_KEYS[category]) : category;
const unitLabel = product => product.unitLabel === 'dozen' ? t('perDozen') : product.unitLabel === 'pack' ? t('perPack') : product.unitLabel === 'displayPack' ? t('perDisplayPack') : t('perUnit');

function persist() { localStorage.setItem('2j-products', JSON.stringify(state.products.filter(product=>String(product.id).startsWith('custom-')))); localStorage.setItem('2j-product-overrides', JSON.stringify(state.overrides)); localStorage.setItem('2j-cart', JSON.stringify(state.cart)); }
function applyTranslations() {
  document.documentElement.lang = state.lang; document.title = t('pageTitle');
  document.querySelectorAll('[data-i18n]').forEach(element => { element.textContent = t(element.dataset.i18n); });
  document.querySelectorAll('[data-i18n-placeholder]').forEach(element => { element.placeholder = t(element.dataset.i18nPlaceholder); });
  document.querySelectorAll('[data-i18n-aria]').forEach(element => { element.setAttribute('aria-label', t(element.dataset.i18nAria)); });
  document.querySelectorAll('[data-lang]').forEach(element => { const active=element.dataset.lang===state.lang; element.classList.toggle('active',active); element.setAttribute('aria-pressed',active); });
}
function setLanguage(lang) { state.lang=lang; localStorage.setItem('2j-lang',lang); applyTranslations(); if(state.editingProductId){$('#productFormTitle').textContent=t('editProduct');$('#saveProductBtn').textContent=t('saveChanges');} renderFilters(); renderCart(); if($('#manageDialog').open)renderAdmin(); if(state.search)doSearch(); }
function categories() { return ['All', ...new Set(state.products.map(product => product.category))]; }
function renderFilters() { $('#categoryFilters').innerHTML=categories().map(category=>`<button class="chip ${state.category===category?'active':''}" data-category="${escapeHtml(category)}" type="button">${escapeHtml(categoryName(category))}</button>`).join(''); }
function interpretQuery(query) {
  const q=query.toLowerCase().trim(); let maxPrice=Infinity;
  const englishPrice=q.match(/(?:under|below|less than|up to)\s*\$?\s*(\d+(?:\.\d+)?)/); const koreanPrice=q.match(/\$?\s*(\d+(?:\.\d+)?)\s*(?:달러)?\s*(?:이하|미만)/);
  if(englishPrice||koreanPrice)maxPrice=Number((englishPrice||koreanPrice)[1]);
  const clean=q.replace(/(?:under|below|less than|up to)\s*\$?\s*\d+(?:\.\d+)?/g,'').replace(/\$?\s*\d+(?:\.\d+)?\s*(?:달러)?\s*(?:이하|미만)/g,'');
  const stops=['show','find','need','with','best','for','the','some','products','product','보여줘','찾아줘','추천','상품','제품','필요'];
  const terms=clean.split(/\s+/).filter(word=>(word.length>2||/[가-힣]/.test(word))&&!stops.includes(word)).map(word=>word.endsWith('s')&&word.length>3?word.slice(0,-1):word);
  return {maxPrice,terms};
}
function filteredProducts() {
  const {maxPrice,terms}=interpretQuery(state.search);
  let list=state.products.filter(product=>{ const haystack=`${product.sourceCode||''} ${product.name} ${product.nameKo||''} ${product.description||''} ${product.descriptionKo||''} ${product.category} ${categoryName(product.category)} ${(product.tags||[]).join(' ')} ${(product.tagsKo||[]).join(' ')}`.toLowerCase(); return (state.category==='All'||product.category===state.category)&&product.price<=maxPrice&&terms.every(term=>haystack.includes(term)); });
  if(state.sort==='price-asc')list.sort((a,b)=>a.price-b.price); if(state.sort==='price-desc')list.sort((a,b)=>b.price-a.price); if(state.sort==='name')list.sort((a,b)=>productName(a).localeCompare(productName(b),state.lang)); return list;
}
function renderProducts() {
  const list=filteredProducts(); $('#resultCount').textContent=t('productCount',{count:list.length}); $('#productGrid').hidden=!list.length; $('#emptyState').hidden=!!list.length;
  $('#productGrid').innerHTML=list.map(product=>{ const inCart=state.cart[product.id]||0; const name=productName(product); const tags=productTags(product)||[]; const description=productDescription(product); const hasStock=Number.isFinite(product.stock); const price=product.pricePending?`<span class="price pending">${t('pricePending')}</span>`:`<span class="price">${money(product.price)} <small>${unitLabel(product)}</small></span>`; return `<article class="product-card" data-product-id="${product.id}"><div class="product-visual ${product.image?'has-photo':''}" style="--visual-bg:${product.color}"><span class="placeholder-label">${product.image?t('driveImage'):t('sampleImage')}</span>${product.image?`<img class="product-photo" src="${escapeHtml(product.image)}" alt="${escapeHtml(name)}" loading="lazy" />`:`<span class="product-icon">${product.icon||'🧴'}</span>`}<span class="stock ${hasStock&&product.stock<10?'low':''}">${hasStock?(product.stock<10?t('onlyLeft',{count:product.stock}):t('inStock',{count:product.stock})):t('catalogItem')}</span></div><div class="product-body"><span class="product-category">${escapeHtml(categoryName(product.category))}</span><h3>${escapeHtml(name)}</h3>${description?`<p class="product-description">${escapeHtml(description)}</p>`:''}<div class="product-tags">${tags.map(tag=>`#${escapeHtml(tag.replace(/\s/g,''))}`).join(' ')}</div><div class="product-bottom">${price}<button class="btn add-btn ${inCart?'added':''}" data-add="${product.id}" type="button" ${product.pricePending?'disabled':''}>${inCart?t('added',{count:inCart}):t('add')}</button></div></div></article>`; }).join('');
}
function cartDetails() { return Object.entries(state.cart).map(([id,qty])=>({product:state.products.find(item=>item.id===id),qty})).filter(line=>line.product&&line.qty>0); }
function renderCart() {
  const lines=cartDetails(); const units=lines.reduce((sum,line)=>sum+line.qty,0); const total=lines.reduce((sum,line)=>sum+line.product.price*line.qty,0);
  $('#cartCount').textContent=units; $('#unitCount').textContent=units; $('#cartTotal').textContent=money(total); $('#heroOrderTotal').textContent=money(total); $('#heroOrderItems').textContent=units?t('selectedUnits',{count:units}):t('noProductsSelected'); $('#reviewBtn').disabled=!units; $('#cartEmpty').hidden=!!lines.length;
  $('#cartItems').innerHTML=lines.map(({product,qty})=>{const name=productName(product);return `<div class="cart-line"><div><div class="cart-name">${escapeHtml(name)}</div><div class="cart-meta">${t('packPrice',{price:money(product.price),unit:unitLabel(product)})}</div></div><div class="cart-line-right"><strong>${money(product.price*qty)}</strong><div class="stepper"><button type="button" data-qty="${product.id}" data-delta="-1" aria-label="${escapeHtml(t('removeOne',{name}))}">−</button><span>${qty}</span><button type="button" data-qty="${product.id}" data-delta="1" aria-label="${escapeHtml(t('addOne',{name}))}">+</button></div></div></div>`;}).join(''); renderProducts();
}
function addToCart(id) { const product=state.products.find(item=>item.id===id); if(!product)return; const next=(state.cart[id]||0)+1; state.cart[id]=Number.isFinite(product.stock)?Math.min(next,product.stock):next; persist(); renderCart(); toast(t('productAdded',{name:productName(product)})); }
function changeQty(id,delta) { const product=state.products.find(item=>item.id===id); const raw=(state.cart[id]||0)+delta; const next=Number.isFinite(product?.stock)?Math.min(raw,product.stock):raw; if(next<=0)delete state.cart[id]; else state.cart[id]=next; persist(); renderCart(); }
function doSearch() { state.search=$('#searchInput').value.trim(); const list=filteredProducts(); renderProducts(); if(state.search){const {maxPrice}=interpretQuery(state.search);const priceText=Number.isFinite(maxPrice)?t('priceLimit',{price:money(maxPrice)}):'';$('#aiResponse').textContent=list.length?t('searchFound',{count:list.length,query:state.search,priceText}):t('searchNone',{query:state.search});$('#aiResponse').classList.add('visible');}else $('#aiResponse').classList.remove('visible'); }
function orderText() {
  const lines=cartDetails(); const total=lines.reduce((sum,line)=>sum+line.product.price*line.qty,0); const empty=t('notProvided');
  return [t('handoffTitle'),`${t('retailShop')}: ${$('#shopName').value||empty}`,`${t('contact')}: ${$('#shopContact').value||empty}`,`${t('salesperson')}: ${$('#salesperson').value||empty}`,`${t('created')}: ${new Date().toLocaleString(state.lang==='ko'?'ko-KR':'en-US')}`,'',...lines.map(({product,qty})=>`${product.sourceCode||product.id} · ${productName(product)} | ${qty} × ${money(product.price)} | ${money(qty*product.price)}`),'',`${t('estimatedTotalUpper')}: ${money(total)}`,`${t('note')}: ${$('#orderNote').value||t('none')}`,'',t('handoffDisclaimer')].join('\n');
}
function csvCell(value) { return `"${String(value??'').replace(/"/g,'""')}"`; }
function orderCsv() {
  const shop=$('#shopName').value; const contact=$('#shopContact').value; const salesperson=$('#salesperson').value; const note=$('#orderNote').value;
  const rows=[['Retail Shop','Contact','Salesperson','Item Number','Product','Description','Quantity','Unit Price','Package Unit','Line Total','Order Note']];
  cartDetails().forEach(({product,qty})=>rows.push([shop,contact,salesperson,product.sourceCode||product.id,productName(product),productDescription(product),qty,product.price,product.unitLabel||'unit',(product.price*qty).toFixed(2),note]));
  return '\ufeff'+rows.map(row=>row.map(csvCell).join(',')).join('\r\n');
}
function openHandoff() {
  const lines=cartDetails(); const total=lines.reduce((sum,line)=>sum+line.product.price*line.qty,0); const empty=t('notProvided'); state.lastSummary=orderText();
  $('#handoffContent').innerHTML=`<div class="handoff-meta"><div><span>${t('retailShop')}</span><strong>${escapeHtml($('#shopName').value||empty)}</strong></div><div><span>${t('contact')}</span><strong>${escapeHtml($('#shopContact').value||empty)}</strong></div><div><span>${t('salesperson')}</span><strong>${escapeHtml($('#salesperson').value||empty)}</strong></div></div><table class="handoff-table"><thead><tr><th>${t('product')}</th><th>${t('qty')}</th><th>${t('unit')}</th><th>${t('lineTotal')}</th></tr></thead><tbody>${lines.map(({product,qty})=>`<tr><td><strong>${escapeHtml(product.sourceCode||product.id)}</strong><br>${escapeHtml(productName(product))}</td><td>${qty}</td><td>${money(product.price)}</td><td>${money(product.price*qty)}</td></tr>`).join('')}</tbody></table><div class="handoff-total">${t('estimatedTotal')} · ${money(total)}</div>${$('#orderNote').value?`<div class="handoff-note"><strong>${t('orderNote')}:</strong> ${escapeHtml($('#orderNote').value)}</div>`:''}<div class="handoff-note"><strong>${t('nextStep')}:</strong> ${t('nextStepText')}</div>`; $('#handoffDialog').showModal();
}
function renderAdmin() {
  const query=state.adminSearch.toLowerCase();
  const visible=state.products.filter(product=>`${product.sourceCode||''} ${product.name} ${product.nameKo||''} ${product.description||''} ${product.category}`.toLowerCase().includes(query));
  $('#adminCount').textContent=t('productCountAdmin',{count:visible.length});
  $('#adminList').innerHTML=visible.map(product=>`<div class="admin-product"><div><strong>${escapeHtml(product.sourceCode||product.id)} · ${escapeHtml(productName(product))}</strong><small>${escapeHtml(categoryName(product.category))} · ${money(product.price)}${product.description?` · ${escapeHtml(product.description.slice(0,55))}`:''}</small></div><div class="admin-actions"><button type="button" class="edit-btn" data-edit="${product.id}">${t('edit')}</button><button type="button" class="delete-btn" data-delete="${product.id}">${t('delete')}</button></div></div>`).join('');
}
function resetProductForm() { state.editingProductId=null; $('#productForm').reset(); $('#productFormTitle').textContent=t('enrollProduct'); $('#saveProductBtn').textContent=t('addCatalog'); $('#cancelEditBtn').hidden=true; }
function beginEdit(id) {
  const product=state.products.find(item=>item.id===id); if(!product)return;
  state.editingProductId=id; $('#newName').value=product.name||''; $('#newCategory').value=product.category||''; $('#newPrice').value=product.price||''; $('#newDescription').value=product.description||''; $('#newTags').value=(product.tags||[]).join(', '); $('#newImage').value='';
  $('#productFormTitle').textContent=t('editProduct'); $('#saveProductBtn').textContent=t('saveChanges'); $('#cancelEditBtn').hidden=false; $('#newName').focus();
}
function imageDataUrl(file) {
  if(!file)return Promise.resolve('');
  return new Promise((resolve,reject)=>{ const image=new Image(); const url=URL.createObjectURL(file); image.onload=()=>{const scale=Math.min(1,720/Math.max(image.width,image.height));const canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(image.width*scale));canvas.height=Math.max(1,Math.round(image.height*scale));canvas.getContext('2d').drawImage(image,0,0,canvas.width,canvas.height);URL.revokeObjectURL(url);resolve(canvas.toDataURL('image/webp',.82));};image.onerror=()=>{URL.revokeObjectURL(url);reject(new Error('Image could not be read'));};image.src=url; });
}
async function saveProduct(event) {
  event.preventDefault(); const palette=['#d9b56e','#a7c6bc','#cfaa96','#b9a9cf','#c995a9']; const name=$('#newName').value.trim(); const tags=$('#newTags').value.split(',').map(value=>value.trim()).filter(Boolean); const image=await imageDataUrl($('#newImage').files[0]);
  const editable={name,nameKo:name,category:$('#newCategory').value.trim().toUpperCase(),price:Number($('#newPrice').value),pricePending:false,description:$('#newDescription').value.trim(),descriptionKo:$('#newDescription').value.trim(),tags,tagsKo:tags}; if(image)editable.image=image;
  if(state.editingProductId){const index=state.products.findIndex(product=>product.id===state.editingProductId);if(index<0)return;const existing=state.products[index];state.products[index]={...existing,...editable};if(String(existing.id).startsWith('custom-')){delete state.overrides[existing.id];}else{state.overrides[existing.id]={...(state.overrides[existing.id]||{}),...editable};}toast(t('productUpdated'));}
  else {const product={id:`custom-${Date.now()}`,sourceCode:`CUSTOM-${Date.now()}`,...editable,stock:24,icon:'🧴',color:palette[state.products.length%palette.length]};state.products.unshift(product);toast(t('productEnrolled'));}
  persist(); renderFilters(); renderCart(); renderAdmin(); resetProductForm();
}
function deleteProduct(id) { const product=state.products.find(item=>item.id===id); const name=product?productName(product):t('product'); state.products=state.products.filter(item=>item.id!==id); delete state.cart[id]; const deleted=JSON.parse(localStorage.getItem('2j-deleted-products')||'[]'); if(!deleted.includes(id)){deleted.push(id);localStorage.setItem('2j-deleted-products',JSON.stringify(deleted));} persist(); renderFilters(); renderCart(); renderAdmin(); toast(t('productDeleted',{name})); }
function toast(message) { const element=$('#toast'); element.textContent=message; element.classList.add('show'); clearTimeout(window.toastTimer); window.toastTimer=setTimeout(()=>element.classList.remove('show'),1800); }
function toggleOrder(show) { $('#orderPanel').classList.toggle('open',show); $('#overlay').classList.toggle('show',show); document.body.style.overflow=show?'hidden':''; }

document.addEventListener('click',event=>{ const lang=event.target.closest('[data-lang]');if(lang)setLanguage(lang.dataset.lang);const add=event.target.closest('[data-add]');if(add)addToCart(add.dataset.add);const qty=event.target.closest('[data-qty]');if(qty)changeQty(qty.dataset.qty,Number(qty.dataset.delta));const chip=event.target.closest('[data-category]');if(chip){state.category=chip.dataset.category;renderFilters();renderProducts();}const edit=event.target.closest('[data-edit]');if(edit)beginEdit(edit.dataset.edit);const del=event.target.closest('[data-delete]');if(del)deleteProduct(del.dataset.delete); });
$('#aiSearchBtn').addEventListener('click',doSearch); $('#searchInput').addEventListener('keydown',event=>{if(event.key==='Enter')doSearch();}); $('#searchInput').addEventListener('input',event=>{if(!event.target.value){state.search='';$('#aiResponse').classList.remove('visible');renderProducts();}}); $('#sortSelect').addEventListener('change',event=>{state.sort=event.target.value;renderProducts();});
$('#clearFiltersBtn').addEventListener('click',()=>{state.search='';state.category='All';$('#searchInput').value='';$('#aiResponse').classList.remove('visible');renderFilters();renderProducts();}); $('#openOrderBtn').addEventListener('click',()=>toggleOrder(true)); $('#closeOrderBtn').addEventListener('click',()=>toggleOrder(false)); $('#overlay').addEventListener('click',()=>toggleOrder(false)); $('#reviewBtn').addEventListener('click',openHandoff); $('#manageBtn').addEventListener('click',()=>{renderAdmin();$('#manageDialog').showModal();}); $('#closeManageBtn').addEventListener('click',()=>$('#manageDialog').close()); $('#productForm').addEventListener('submit',saveProduct); $('#cancelEditBtn').addEventListener('click',resetProductForm); $('#adminSearch').addEventListener('input',event=>{state.adminSearch=event.target.value.trim();renderAdmin();});
$('#copyOrderBtn').addEventListener('click',async()=>{await navigator.clipboard.writeText(state.lastSummary);toast(t('summaryCopied'));}); $('#downloadOrderBtn').addEventListener('click',()=>{const blob=new Blob([state.lastSummary],{type:'text/plain'});const anchor=document.createElement('a');anchor.href=URL.createObjectURL(blob);anchor.download=`2J-order-${Date.now()}.txt`;anchor.click();URL.revokeObjectURL(anchor.href);toast(t('summaryDownloaded'));});
$('#downloadCsvBtn').addEventListener('click',()=>{const blob=new Blob([orderCsv()],{type:'text/csv;charset=utf-8'});const anchor=document.createElement('a');anchor.href=URL.createObjectURL(blob);anchor.download=`2J-order-${Date.now()}.csv`;anchor.click();URL.revokeObjectURL(anchor.href);toast(t('csvDownloaded'));});

applyTranslations(); renderFilters(); renderCart();
